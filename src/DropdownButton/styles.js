import css from 'styled-jsx/css'

export default css`
    div {
        display: inline-flex;
        position: relative;
        color: inherit;
        white-space: nowrap;
    }

    .menu {
        z-index: 1000;
        position: absolute;
        top: 38px;
        left: 0;
    }
`
